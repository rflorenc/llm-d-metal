apiVersion: inference.networking.k8s.io/v1
kind: InferencePool
metadata:
  name: vllm-metal-pool
spec:
  selector:
    matchLabels:
      app: vllm-metal-pool
  endpointPickerRef:
    name: vllm-metal-pool
    kind: Service
    port:
      number: 9002
  targetPorts:
  - number: 8000
