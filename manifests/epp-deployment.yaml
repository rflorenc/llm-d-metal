apiVersion: apps/v1
kind: Deployment
metadata:
  name: vllm-metal-epp
  namespace: llm-d-metal
  labels:
    app: vllm-metal-epp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: vllm-metal-epp
  template:
    metadata:
      labels:
        app: vllm-metal-epp
    spec:
      serviceAccountName: vllm-metal-epp
      terminationGracePeriodSeconds: 130
      containers:
      - name: epp
        image: ghcr.io/llm-d/llm-d-inference-scheduler:latest
        imagePullPolicy: IfNotPresent
        args:
        - --pool-name
        - vllm-metal-pool
        - --pool-namespace
        - llm-d-metal
        - --v
        - "4"
        - --zap-encoder
        - "json"
        - --grpc-port
        - "9002"
        - --grpc-health-port
        - "9003"
        - --config-file
        - "/etc/epp/epp-config.yaml"
        - --metrics-endpoint-auth=false
        ports:
        - containerPort: 9002
        - containerPort: 9003
        - name: metrics
          containerPort: 9090
        livenessProbe:
          grpc:
            port: 9003
            service: envoy.service.ext_proc.v3.ExternalProcessor
          initialDelaySeconds: 5
          periodSeconds: 10
        readinessProbe:
          grpc:
            port: 9003
            service: envoy.service.ext_proc.v3.ExternalProcessor
          initialDelaySeconds: 5
          periodSeconds: 10
        volumeMounts:
        - name: epp-config
          mountPath: /etc/epp
        - name: cache
          mountPath: /cache
        - name: uds-socket
          mountPath: /tmp/tokenizer
      # NOTE: UDS tokenizer sidecar omitted for POC.
      # Prefix-cache scoring (which requires tokenization) won't work,
      # but basic load-balanced routing will.
      volumes:
      - name: epp-config
        configMap:
          name: epp-config
      - name: cache
        emptyDir: {}
      - name: uds-socket
        emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: vllm-metal-pool-epp
  namespace: llm-d-metal
spec:
  selector:
    app: vllm-metal-epp
  ports:
  - name: ext-proc
    protocol: TCP
    port: 9002
    targetPort: 9002
    appProtocol: http2
  type: ClusterIP
